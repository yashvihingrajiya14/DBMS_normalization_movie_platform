# DBMS_normalization_movie_platform
CineDB — A full-stack DBMS web app that performs step-by-step normalization analysis (1NF → 2NF → 3NF → BCNF) on relational schemas. Features attribute closure, candidate key detection, violation analysis, and schema decomposition — built on an Indian OTT streaming dataset with Oracle SQL + Flask + Vanilla JS.
